const INITIAL_STATE = {
    cars: []
};

const auctionsReducer = (state = INITIAL_STATE, action) => {
    switch (action.type) {
        default:
            return state;
    }
};

export default auctionsReducer;